package integerarrayssort;

public class SequentialSort {
    public static Integer[] execute(Integer[] integers) {
        SortAlgorithm sortAlgorithm = new SortAlgorithm();
        return sortAlgorithm.sort(integers);
    }
}
